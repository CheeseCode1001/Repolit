Changes to be made to the app

1. Add anonymous user sessions and trial access control to my existing app.

Requirements:

i. Anonymous User Identification:
- When a user first visits the app, generate a unique UUID (anon_id)
- Store it securely in a cookie (HttpOnly if possible) or localStorage
- If the user already has an anon_id, reuse it

ii. Backend Handling:
- Treat anon_id as a user identifier (like a temporary account)
- Ensure every request includes this ID
- If no ID is present, generate one and return it to the client

iii. Data Isolation:
- Modify database schema so all user-generated data includes a user_id field
- For anonymous users, user_id = anon_id
- Update all read/write queries to filter strictly by user_id
- Ensure no user can access another user's data

iv. Trial Logic:
- Track created_at for each anonymous user
- Add trial limits:
  - Example: max 10 actions OR 24-hour access
- Block or restrict access when limits are exceeded

v. Upgrade to Account:
- When a user signs up/logs in:
  - Create a real user account
  - Migrate all data from anon_id to the new user_id
  - Clear or deactivate the anonymous session

vi. Security:
- Do not trust user-submitted IDs directly
- Always validate user identity on the backend
- Use secure cookies where possible

vii. Keep changes minimal:
- Integrate into existing routes and database
- Do not rewrite the whole app
- Follow the current project structure and conventions

Output:
- Show updated backend routes/middleware
- Show any schema changes
- Show how the frontend stores/sends the anon_id
- Keep code simple and well-commented

-——————————-

1. Configure the OAuth credentials specifically for the Production environment in your project's Auth pane.

———————————-

1. Make the platform gamified by assigning points to the user for every repo they scan 

If a user is on the free tire they can use ther it points to scan more repos (users on the free tire are only allowed to scan two repos and if tye want to scan more they would have to use points)

Users who do not create account are only allowed to scan 1 repo and they can’t earn points unless they are registered 

————————————

1. Make sure the dark and light mode affect every component 

Make sure the registered users can shear the scanned repos

————————-

1. Create a profile icon at the header and when the user click on the profile icon they go to their profile page where they can change their name, avatar, add user name and perform other actions like connecting with GitHub 

In the profile page there should be a “Back to Home” button to redirect users to the main home page

User this libraries for the icons 

React-nice-avatar 
npm install react-nice-avatar

-————————————

Aside from pasting the git hub link users can also :

Upload Folder --- you can Upload your folder and get started
Open Zip Folder --- you can Open a Zip folder and get started
Shear Analysis with team 

The app should also be able to Works offline — analyze local files without internet